import React from "react";

// Shell (Sidebar + TopBar) is provided by the parent /app/layout.tsx
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
